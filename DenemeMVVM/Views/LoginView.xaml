<UserControl x:Class="DenemeMVVM.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DenemeMVVM.Views" 
             xmlns:helix="http://helix-toolkit.org/wpf"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid>
            <Grid VerticalAlignment="Center" HorizontalAlignment="Center" Height="300" Width="200">
                <TextBlock Text="UserId" Height="15" Margin="20,85,144,200"/>
                <TextBox Text="{Binding UserId, UpdateSourceTrigger=PropertyChanged}" Height="15" Width="70" Margin="20, 100, 110, 185"/>
                <TextBlock Text="Password" Height="15" Margin="20,135,130,150"/>
                <TextBox Text="{Binding Password, UpdateSourceTrigger=PropertyChanged}" Height="15" Width="70" Margin="20, 150, 110, 135"/>
                <Button Command="{Binding LoginCommand}" Content="Login" Height="20" Width="50" Margin="75,202,75,78"/>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
